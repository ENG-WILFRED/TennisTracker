import 'package:flutter/material.dart';

class KnockoutPage extends StatelessWidget {
  const KnockoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Knockout')),
      body: const Center(child: Text('Knockout bracket placeholder')),
    );
  }
}
